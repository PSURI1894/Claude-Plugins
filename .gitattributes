# Normalize line endings: store everything as LF in the repo so the plugin's
# Python shebangs and markdown frontmatter stay valid on macOS/Linux.
* text=auto eol=lf

# Treat images as binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
